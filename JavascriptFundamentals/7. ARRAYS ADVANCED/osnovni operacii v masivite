push() - add to the end
pop() - remove from the end
unshift() - add to the beginning
shift() - remove from the beginning
тези горе всичките са мутиращи функции! т.е променят оригиналния масив!

    slice()  - немутираща - прави нов масив като реже оригиналния - пример slice(2, 4) ще отреже от 2ри индекс до 4ти. Тук особеност е, че началото е включено, а краят не, 
    т.е в новия масив ще има 2 и 3
    също може да се ползва за клониране на масив!

splice() - мутираща функция! виж demoSplice.js
може и да вмъква вътре в масива, без да трие - с нула на втория оператор.

    filter() - немутираща; филтрира

    map()    - немутираща; променя всеки един елемент от масива

sort() - мутираща функция!

localeCompare() - ползва се за сортиране
    пример a.localeCompare(b)

reverse() - мутираща функция!